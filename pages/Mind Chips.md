- Aperas dev
  collapsed:: true
	- The SKILL.md:
		- > Each item is marked [current], [superseded] (the cause has since been removed — kept so the old advice isn't re-derived), or [unverified] (described, not yet confirmed live).
			- These tags have been obsoleted by the discussion/aperas-skill.md. If kept, i think only `[unverified]` is worth, but until now, nothing is marked `[unverified]` yet.
		- > A projection can be regenerated from the graph at any time. It is never the place a change is made. Editing a projection to change the graph is mistaking the shadow for the thing casting it
			- The very first part still inclines to the write side and misses the read side. Searching and reading should also not from the shadow.
		- > 2. Orientation > ### Deep read runs in two directions
			- This section should be reworded to include both deep read & deep write
			- > Provenance sits inside context, not against it; it needs saying because it is the half that was missing, which is why a node with no sources reads as merely under-linked instead of unsupported.
				- Why put the history here? A new agent cannot know which "was missing" and why "against" or "not against". In the plan, i accepted such phrases because they have **context**. But here, they are not acceptable, because they are out of context! Such a basic skill of context-awareness is lacking right in the writing of the skill. In the single SKILL.md, no links provide context, but the very block containing it, and ones above it are the context. Let's fix the context to be inclusive and not lacking, instead of leaving the old one then write a new paragraph to "correct" it!
				- While deeper content is forward and downward, the context is already what's upward and the deeper one is the backward. So looking upward in the tree is just a basic skill of checking context. We seem to lack such a basic skill in the SKILL.md. The reason the agent like to read the artifact more than the node is usually for broader context. Instead of the full artifact context, using Apeiron, the agent can check context large enough in the tree, e.g. for node `discussion/a.md/h1/freeflow/a/aa/aaa`, the agent can check only `discussion/a.md/h1/freeflow/a` and see it's enough.
		- > 3. Discipline > The loop
			- > Not "the edit loop".
				- Again, out of context! :(( Remove that historical scar, plz.
			- > Citation direction: [current] Generalized after a milestone needed exactly such a pointer and the rule, stated as "designated index," named an instance where it meant a property.
				- This history isn't worth keeping. The same as historical scars above.
	- etc
		- > Never reconstruct by copying from an already-projected file: it has anchor tags spliced in that are a projection artifact, not content, and piping them back bakes them into the block's stored text as prose.
		  => Does this break the round trip? If yes, must be fixed.
		- Can `update` promote placeholder? Revise ### Sketching a structure with placeholder, and add placeholder semantics to `--help`.
		- The list edit command must be revised to be clean, because it's cucial for the unbounded tree.
		- `profile --list-view` => `list-views` to be natural
		- Add deep path to `unfold`/`fold`
		- Lead-in term: bold-colon first then, punctuation heuristic, last is length limit.
		  + Some are longer than the limit, e.g.: `The provenance obligation is scoped to promotion, so a claim added to an entry already in place carries no source:`
		- Linking
			- What's the conversion of address for? What's the form of address in Apeiron?
		- The issue of checkbox: Let's use this case to gauge how many steps you do to find it and its complicated history of back and forth. 
		  > content landing on a tombstoned id, invisible, while the summary reports a clean match
		  
		  It's obviously not this one.
- ĐK AL 14/9
- ---
- 🤔😊😁 😉 😮 😛 🤪 😜 🤣 🙁 😱 👺 👁️🧿🪬  – × → ← ↓ ⇒ ⇋ ⇄ ∞∝α ‘’ ≈ ≥
- Ω-thread Unïnfo