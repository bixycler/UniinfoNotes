# Fix: `resolveTitleReferences()` corrupting titled links & false circular refs

## Problem

`PAT_REF` in `resolveTitleReferences()` (common-utils.js:354) matches `((uuid))` blindly — even inside markdown links `[text](((uuid)))`. Two bugs:

1. **Title corruption**: Replacement turns `[text](((uuid)))` into `[text]RESOLVED_TITLE` — broken link syntax.
2. **False circular refs**: UUIDs inside titled links are added as dependency edges, creating phantom cycles that trigger the "Circular refs detected" fallback.

## Scope boundary

`PAT_REF` is a **local variable** inside `resolveTitleReferences()` — not exported. Only used at lines 363, 394, 429 of `common-utils.js`. The function is called from:
- `build-index.js:147` — global index build
- `logseqmd2commonmark.js:500` — per-file conversion

No other code is affected. The converter's link-rendering patterns (`PAT_UUID_REF`, `PAT_BRACKET_UUID_REF`, `PAT_LINK_REF`) are separate and unchanged.

## Fix (common-utils.js:354)

Add negative lookbehind `(?<!\]\() ` to the `((uuid))` alternative:

```js
// Before:
const PAT_REF = /\(\(([0-9a-fA-F-]{36})\)\)|\[\[([0-9a-fA-F-]{36})\]\]/g;
// After:
const PAT_REF = /(?<!\]\()\(\(([0-9a-fA-F-]{36})\)\)|\[\[([0-9a-fA-F-]{36})\]\]/g;
```

In `[text](((uuid)))`, the inner `((uuid))` is always preceded by `](` — the lookbehind excludes it. Bare `((uuid))` has no preceding `](`, so it's matched normally. The `[[uuid]]` alternative is unchanged.

## Test cases (logseqmd2commonmark-test.md)

Append two new sections after existing section 7:

### Section 8: True positive circular reference (should still trigger warning)
```markdown
- 8.1 Target A: ((55555555-5555-5555-5555-555555555552))
  id:: 55555555-5555-5555-5555-555555555551
- 8.2 Target B that references A back: ((55555555-5555-5555-5555-555555555551))
  id:: 55555555-5555-5555-5555-555555555552
```
Expect: "Circular refs" warning logged. 8.1 and 8.2 resolve with bare UUIDs preserved for the circular cross-reference (fallback behavior unchanged).

### Section 9: False positive circular ref via titled link (cycle only exists because of the titled link)
```markdown
- 9.1 Target A that titled-links to B: [see B](((66666666-6666-6666-6666-666666666662)))
  id:: 66666666-6666-6666-6666-666666666661
- 9.2 Target B that references A back: ((66666666-6666-6666-6666-666666666661))
  id:: 66666666-6666-6666-6666-666666666662
```
Without fix: dependency graph has 9.1→9.2 (false, from titled link) + 9.2→9.1 (real) → falsely circular.
With fix: titled link edge excluded → only 9.2→9.1 → resolves normally.
Expect: No "Circular refs" warning. 9.2 resolves with 9.1's title preserving `[see B](((6666...662)))` intact.

## Verification steps

```bash
# 1. Rebuild global index (may have stale corrupted titles)
node assets/HTML/build-index.js

# 2. Convert test fixture
node assets/HTML/logseqmd2commonmark.js \
  -i index.json \
  -o ~/tmp/logseq/ \
  -b assets/HTML \
  assets/HTML/logseqmd2commonmark-test.md \
  --break br

# 3. Check titled link preserved in resolved block title (false positive test)
grep '66666666-6666-6666-6666-666666666662' ~/tmp/logseq/logseqmd2commonmark-test.cm.md
# Line 51: [see B](#66666666-...) — titled link converted by PAT_LINK_REF
# Line 52: [see B](((66666666-...))) — titled link PRESERVED inside resolved title text

# 4. Check no false circular warning for the 6666 series (only 5555)
# (run step 2 again and verify stderr only shows 5555...51/552)

# 5. Verify index.json entries not corrupted
grep -A2 '"66666666-6666-6666-6666-666666666661"' index.json
# title field should be exactly "9.1 Target A that titled-links to B: [see B](((66666666-6666-6666-6666-666666666662)))"

# 6. Verify existing test case 7 still passes (bare ((uuid)) resolution)
grep '22222222-2222-2222-2222-222222222222' ~/tmp/logseq/logseqmd2commonmark-test.cm.md
# Should show [7.1 Target block for local reference](#22222222-2222-2222-2222-222222222222)
```
