# Plan: Scrape Entire Website `www.sensibleuniversemodel.com`

## Phase 1: URL Discovery & Mapping
- [x] Discover internal links by traversing main sections (`/`, `/psx-archive/`, `/ego-sum/`).
- [x] Finalize the URL list and map them to local paths.
- [x] Create directory structure in `assets/Philosophy/SUM/sensibleuniversemodel.com/`.

## Phase 2: Implementation of `crawl_site.js`
- [x] Develop a Node.js controller script (implemented as batch-processed CDP scripts).
- [x] Ensure `scraper_cdp.js` and `process_extracted.js` are robust for sub-pages.
- [x] Verify that navigation and boilerplate are consistently excluded across different page layouts.

## Phase 3: Execution & Scrutiny Points
- [x] Execute the crawl across 29 identified URLs.
- [x] Handle Jimdo-specific navigation components (`jimdo-header`, `jimdo-nav`).
- [x] Fix "leak" bugs in nested formatting by distinguishing bold (`**`) and italic (`_`).

## Phase 4: Final Verification
- [x] Audit the resulting directory tree.
- [x] Ensure `SUM.md` is the correctly processed homepage.
- [x] Verify that math notation and formatting are consistent across the site.

## Verification Results
- All ~29 pages scraped and structured.
- Local directory tree matches site hierarchy.
- No formatting leaks or navigation boilerplate.

