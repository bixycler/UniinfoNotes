# Plan: Scrape Entire Website `www.sensibleuniversemodel.com`

## Phase 1: URL Discovery & Mapping
- [x] Discover internal links by traversing main sections (`/`, `/psx-archive/`, `/ego-sum/`).
- [ ] Finalize the URL list and map them to local paths:
    - Root (`/`) -> `SUM.md`
    - Paths like `/a/b/` -> `a/b.md`
- [ ] Create directory structure in `assets/Philosophy/SUM/sensibleuniversemodel.com/`.

## Phase 2: Implementation of `crawl_site.js`
- [ ] Develop a Node.js controller script that:
    - Uses the existing `scraper_cdp.js` logic (injected via CDP).
    - Uses the existing `process_extracted.js` logic for post-processing.
    - Manages navigation across the URL list.
    - Ensures directories are created using `mkdir -p`.

## Phase 3: Execution & Scrutiny Points
- [ ] Execute the crawl.
- [ ] **Scrutiny Point**: Verify the first 3 pages scraped to ensure no "leaks" or structural breaks before finishing the rest.
- [ ] Handle any Jimdo-specific navigation components found in sub-pages.

## Phase 4: Final Verification
- [ ] Audit the resulting directory tree.
- [ ] Ensure `SUM.md` is the correctly processed homepage.
- [ ] Verify that math notation and formatting are consistent across the site.

## Verification
- [ ] `ls -R assets/Philosophy/SUM/sensibleuniversemodel.com/` to verify structure.
- [ ] Audit random Markdown files for quality.
